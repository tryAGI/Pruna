# Pruna.CLI

Command-line interface for the Pruna SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Pruna.CLI --prerelease
```

## Usage

```bash
pruna --help
pruna api --help
```